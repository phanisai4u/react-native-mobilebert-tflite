# react-native-mobilebert-tflite

A React Native wrapper for MobileBERT Q&A using TensorFlow Lite. Supports iOS (Swift) and Android (Kotlin).

## 📦 Installation

```bash
npm install react-native-mobilebert-tflite
cd ios && pod install
